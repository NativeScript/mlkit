platform :ios, '15.5.0'

if ENV.key?('NSSkipMLKitFromIOSSimulator')
  puts '⏭️  Skipping GoogleMLKit/PoseDetection (simulator build)'
else
  if defined?($NSMLKitPoseDetectionAccurate)
    pod 'GoogleMLKit/PoseDetectionAccurate'
  else
    pod 'GoogleMLKit/PoseDetection'
  end
end
